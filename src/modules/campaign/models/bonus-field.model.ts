export enum PropertyFieldTypeEnum {
  INTEGER = 'integer',
  DECIMAL = 'decimal',
  MONEY = 'money',
  BOOLEAN = 'boolean',
  STRING = 'string',
  DATE_TIME = 'dateTime',
  PROVIDER_GAME_TYPE_GAME_SELECT = 'providerGameTypeGameSelect',
}
